{
  "name": "subtitlebot",
  "private": true,
  "description": "Raiz do repositório — a app Next.js vive em web/",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web",
    "seed": "npm run seed --prefix web"
  }
}
